@model Zakar.ViewModels.CurrencyViewModel

@using (Html.BeginForm("CurrencyEdit", "Setup", FormMethod.Post))
{
    @Html.HiddenFor(m => m.Id)
    @Html.Partial("EditorPartials/_currencyEditorPartial", Model)
}